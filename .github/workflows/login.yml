name: Monthly Login Keepalive

on:
  schedule:
    - cron: '0 0 1 * *'  # 每月1号 UTC时间 00:00 执行
  workflow_dispatch:      # 可手动触发

jobs:
  login:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout 代码
        uses: actions/checkout@v3

      - name: ⚙️ 设置 Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: 📦 安装 Playwright
        run: npm install playwright

      - name: 🚀 执行登录脚本
        env:
          USERNAMES: ${{ secrets.USERNAMES }}
          PASSWORDS: ${{ secrets.PASSWORDS }}
        run: node login.js
